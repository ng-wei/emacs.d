;; initialize fonts
;; init-fonts.el

(setq window-system-default-frame-alist
      '((x (font . "文泉驿等宽微米黑 11"))  ;; frame created in X
	(nil)))                           ;; frame created in terminator

(provide 'init-fonts)
